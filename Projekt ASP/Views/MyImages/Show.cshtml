@model Projekt_ASP.Models.ImageModel

@{
    ViewData["Title"] = "Show";
}

<div>
    <hr />
    <dl class="row">
        <dd class="col-sm-10">
            <img src="@("~/image/" + Model.ImageName)" asp-append-version="true" width="100%" height="100%" />
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ImageID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
